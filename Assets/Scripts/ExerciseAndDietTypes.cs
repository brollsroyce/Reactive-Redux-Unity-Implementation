// Place this in a separate script, e.g. "ExerciseAndDietTypes.cs"
public static class ExerciseAndDietTypes
{
    // Enum for Exercise suggestions
    public enum ExerciseType
    {
        None,
        LightWalking,
        Yoga,
        Cardio,
        StrengthTraining
    }

    // Enum for Diet suggestions
    public enum DietType
    {
        None,
        IronRichFoods,
        ProteinRichFoods,
        LowCarb,
        BalancedDiet,
        MagnesiumRichFoods, // Added for PMS
        HydratingFoods      // Added for PMS
    }
}
